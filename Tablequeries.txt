SQL Table queries:

String sql = "CREATE TABLE Film " +
		"(film_ID INTEGER not NULL, " +
		" titel VARCHAR(255) not NULL, " +
		" director VARCHAR(100) not NULL, " +
		" producer VARCHAR(100) not NULL, " +
		" writer VARCHAR(100) not NULL, " +
		" genre VARCHAR(100) not NULL, " +
		" tijdsDuur INTEGER," +
		" ProductieTijd INTEGER, " +
		" PRIMARY KEY ( film_ID ))";

String sql = "CREATE TABLE Persoon " +
		"(persoon_ID INTEGER not NULL, " +
		" naam VARCHAR(100) not NULL, " +
		" biografie VARCHAR(1000), " +
		" alias VARCHAR(100), " +
		" geboortedatum DATE not NULL, " +
		" geboorteplaats VARCHAR(100), " +
		" rol VARCHAR(100)," +
		" type VARCHAR(100), " +
		" PRIMARY KEY ( persoon_ID ))";
		
String sql = "CREATE TABLE serie " +
		"(serie_ID INTEGER not NULL, " +
		" titel VARCHAR(255) not NULL, " +
		" seizoenen INTEGER , " +
		" PRIMARY KEY ( serie_ID ))";
		
String sql = "CREATE TABLE seizoen " +
		"(seizoen_ID INTEGER not NULL PRIMARY KEY , " +
		" seizoenenNR INTEGER not NULL, " +
		" serie_ID INTEGER not NULL , " +
		"FOREIGN KEY (serie_ID)" +
		"REFERENCES serie (serie_ID))";

String sql = "CREATE TABLE afleveringVan " +
		"(afleveringV_ID INTEGER not NULL PRIMARY KEY , " +
		" seizoen_ID INTEGER not NULL, " +
		" film_ID INTEGER not NULL , " +
		"FOREIGN KEY (seizoen_ID)" +
		"REFERENCES seizoen (seizoen_ID)," +
		"FOREIGN KEY (film_ID)" +
		"REFERENCES film (film_ID))";
		
String sql = "CREATE TABLE gewerktAan " +
		"(gewerktAan_ID INTEGER not NULL PRIMARY KEY , " +
		"persoon_ID INTEGER not NULL, " +
		"film_ID INTEGER not NULL, " +
		"rol VARCHAR(100) not NULL , " +
		"FOREIGN KEY (persoon_ID)" +
		"REFERENCES persoon (persoon_ID)," +
		"FOREIGN KEY (film_ID)" +
		"REFERENCES film (film_ID))";
